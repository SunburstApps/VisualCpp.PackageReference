<Project>
  <!-- I cannot conditionalize an <Sdk /> reference. Therefore, I must move the condition here. -->
  <Import Project="$(MSBuildThisFileDirectory)\..\tools\Sunburst.VisualCpp.PackageReference.props"
          Condition="'$(MSBuildProjectExtension)' == '.vcxproj'" />
</Project>
